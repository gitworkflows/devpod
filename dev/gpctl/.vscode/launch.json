{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "gpctl",
            "type": "node",
            "request": "launch",
            "program": "${workspaceFolder}",
            "args": [
                "clusters",
                "list"
            ]
        }
    ]
}
